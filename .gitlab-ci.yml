image: aneoshun/airl_build_env:latest

stages:
  - build

singularity-image:
  stage: build
  script:
     - /bin/bash ./build_base.sh --nofakeroot
     - /bin/bash ./push_image.sh --uri library://airl_lab/default/airl_env --tag base_ci --cli registry
       